---
title: CLIP-Guided Label-Free Discriminative Region Scoring for Fine-Grained Classification
title_zh: CLIP引导的无标签判别性区域评分用于细粒度分类
authors: Yujie Zhu
date: 2026-07-15
pdf: "https://arxiv.org/pdf/2607.13437v1"
tags: ["query:dino-fg"]
score: 9.0
evidence: CLIP引导的无标签判别区域评分用于细粒度分类
tldr: 针对细粒度分类中缺乏标签时如何选择判别性区域的问题，提出统一的无标签区域评分框架。使用CLIP和SAM生成区域并比较余弦相似度、边缘和熵三类评分策略，引入基于全局和局部嵌入的伪标签变体。实验表明，软负边缘评分最优，伪标签逼近真实标签性能；随机裁剪优于SAM，因其保留上下文信息。SAM适合聚合所有区域，随机裁剪适合小top-k子集，为细粒度分类提供新见解。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.13437v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1095, \"height\": 1002, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.13437v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1428, \"height\": 945, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.13437v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1444, \"height\": 827, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13437v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 325, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13437v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1441, \"height\": 1074, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.13437v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 1074, \"label\": \"Table\"}]"
motivation: 现有方法依赖标签或固定策略，无法在无标签情况下选择最优的判别性区域和评分策略。
method: 提出统一框架，结合SAM掩膜与随机裁剪，使用余弦相似度、边缘和熵三种评分策略，并引入全局/局部伪标签变体。
result: 软负边缘评分最优，伪标签评分逼近真实标签；随机裁剪全面优于SAM，且SAM需全区域聚合而随机裁剪适合小top-k。
conclusion: 随机裁剪因保留上下文更适合无标签场景，为细粒度分类提供新基准与方向。
---

## 摘要
最近的视觉模型（如CLIP和SAM）能够实现无训练的分割和语义编码，用于细粒度分类。一种常见方法是将分割图像区域的表示与对应标签的文本提示嵌入进行比较。然而，不同的局部区域和基于CLIP的评分策略如何影响判别性证据的选择仍不清楚，尤其是在缺乏真实标签的情况下。本文提出了一种统一的CLIP引导的无标签区域评分框架，用于细粒度分类。该框架使用SAM生成的掩码和随机裁剪，评估了基于余弦相似度、基于边际和基于熵的评分策略，并引入了两种基于全局图像嵌入和局部区域嵌入的无标签伪标签变体。我们在五个细粒度分类数据集上进行了实验，系统比较了不同的区域生成方法和评分策略。结果表明，软负边际评分（Soft Negative Margin）取得了最佳性能，且伪标签评分接近真实标签的性能。尽管SAM生成语义上有意义的掩码，但在所有数据集上，基于随机裁剪的伪标签评分始终优于基于SAM的评分，这表明在伪标签存在噪声时，随机裁剪保留了周围信息并提供了更稳定的语义上下文。此外，SAM掩码受益于聚合所有区域的嵌入，而随机裁剪倾向于在较小的top-k子集上表现更好。这些发现为细粒度分类提供了新的见解。

## Abstract
Recent vision models such as CLIP and SAM enable training-free segmentation and semantic encoding for fine-grained classification. A common approach is to compare the representations of segmented image regions with the text prompt embeddings of the corresponding labels. However, it remains unclear how different local regions and CLIP-based scoring strategies affect the selection of discriminative evidence, especially when ground-truth labels are unavailable. In this paper, we propose a unified CLIP-guided label-free region scoring framework for fine-grained classification. The framework evaluates cosine similarity-based, margin-based, and entropy-based scoring strategies using both SAM-generated masks and random crops, and introduces two label-free pseudo-label variants based on global image embeddings and local region embeddings. We conduct experiments on five fine-grained classification datasets to systematically compare different region generation methods and scoring strategies. The results show that Soft Negative Margin scoring achieves the strongest performance, and pseudo-label scoring closely approximates true-label performance. Although SAM produces semantically meaningful masks, random-crop-based pseudo-label scoring consistently outperforms SAM-based scoring across all datasets, suggesting that random crops preserve surrounding information and provide more stable semantic context when pseudo-labels are noisy. In addition, SAM masks benefit from aggregating embeddings from all regions, whereas random crops tend to perform better with a smaller top-k subset. These findings provide new insights for fine-grained classification.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **问题**：细粒度分类（Fine-grained Classification）中，不同子类视觉相似度高，传统方法依赖人工标注的边界框或关键点，或需要复杂定位模块。近期的大规模视觉-语言模型（如CLIP）和分割模型（如SAM）虽能实现无训练的特征提取，但如何在没有真实标签的情况下（标签不可用）有效选择判别性局部区域、以及不同评分策略（余弦相似度、边际、熵等）如何影响性能，尚未被系统研究。
- **动机**：现有方法通常直接比较所有区域与类别文本，缺乏针对性的区域排序与选择机制。作者提出一个**统一的CLIP引导无标签区域评分框架**，旨在无需任何标签的条件下，自动判别并聚合最富有判别力的局部区域，提升细粒度分类准确率。
- **整体含义**：该工作填补了无标签场景下细粒度分类区域选择策略的系统性评估空白，为未来轻量级、无监督的细粒度识别提供新基准。

## 2. 方法论：核心思想、关键技术细节
- **核心思想**：利用冻结的CLIP和SAM生成候选局部区域，通过多种评分策略估计每个区域的判别性，选择top-k区域，聚合其嵌入，并与全局特征拼接后送入线性分类器（仅分类头可训练）。
- **流程**：输入图像 → 生成L个候选区域（SAM掩膜或随机裁剪） → 提取全局嵌入g、区域嵌入fi、类别文本嵌入tc → 对每个区域计算评分si → 选出top-k区域 → 通过softmax权重聚合得到局部表示r → 拼接[g; r] → 线性分类器 → 交叉熵损失训练。
- **关键评分策略**：
  - **Region-Global Similarity**：余弦相似度 s_i = cos(fi, g)
  - **Region-Text Similarity**：s_i = cos(fi, t_y) （y为真实或伪标签）
  - **Hard Negative Margin**：s_i = cos(fi, t_y) - max_{c≠y} cos(fi, t_c)
  - **Soft Negative Margin**：用温度τ softmax加权所有负类，s_i = cos(fi, t_y) - Σ_{c≠y} α_{i,c} cos(fi, t_c)
  - **Entropy Confidence**：s_i = p_i(y) - λ H(p_i)，其中p_i为区域对各类的softmax概率，H为熵。
- **无标签变体**：提出两种伪标签：
  - **全局伪标签**：用全局嵌入g的top-1预测作为ỹ。
  - **局部伪标签**：每个区域独立预测自己的ỹ_i。
- **区域聚合**：选中的top-k区域，用softmax权重（基于评分）加权平均得到r。最终特征为[g; r]，使用线性分类头+交叉熵。

## 3. 实验设计
- **数据集**：5个细粒度分类基准
  - CUB-200-2011（200种鸟类，11788张图）
  - Oxford 102 Flowers（102种花）
  - Oxford-IIIT Pets（37种猫狗，约200张/类）
  - Stanford Cars（196种车辆，16185张）
  - FGVC Aircraft（100种飞机型号，10000张）
- **Benchmark**：冻结的CLIP ViT-B/32 + SAM ViT-B。对比方法包括：
  - 全局线性探针（无局部区域）
  - 平均所有区域（无评分）
  - 不同评分策略（余弦相似度、硬负边缘、软负边缘、熵置信度）
  - 真实标签 vs 全局伪标签 vs 局部伪标签
  - 两种候选区域：SAM掩膜 vs 随机裁剪
- **消融实验**：top-k敏感性（k=2~7及全部），结果见表3、4。

## 4. 资源与算力
- 文中**未明确说明**使用的GPU型号、数量、训练时长。
- 实现细节：CLIP ViT-B/32，SAM ViT-B，训练30个epoch，batch size 32，AdamW优化器，学习率1e-3，权重衰减1e-4，dropout 0.1。所有嵌入L2归一化。
- 仅提及SAM预处理时间大约是随机裁剪的10倍以上，但具体绝对时间未给出。

## 5. 实验数量与充分性
- **数量**：主要实验表1（5数据集 × 2候选类型 × 若干评分策略，共约40组），表2（真实标签 vs 伪标签对比），表3和表4（top-k消融，每个评分策略7种k值）。共计大量实验。
- **充分性与客观性**：
  - 覆盖了主流细粒度数据集，区域生成方式、评分策略、标签来源三个维度全面比较。
  - 使用相同最大区域数和面积范围以保证公平。
  - 所有模型仅训练线性分类头，冻结视觉编码器，控制变量严格。
  - 但缺少对更先进CLIP骨干（如ViT-L）或不同温度参数的消融，也未测试其他区域生成方法（如非矩形裁剪）。总体实验设计扎实、结果可信。

## 6. 主要结论与发现
- **软负边缘评分（Soft Negative Margin）** 在所有无标签评分中表现最强，说明判别性区域不仅需与目标类对齐，还需与相似类别分离。
- **伪标签评分** 的性能非常接近真实标签评分（在多个数据集上差距小于2%），表明全局/局部伪标签足够可靠。
- **随机裁剪全面优于SAM掩膜**（所有5个数据集上），尽管SAM生成语义上有意义的掩膜。原因：随机裁剪保留了上下文信息，而SAM掩膜缺乏周围信息且可能引入纯背景噪声。
- **SAM掩膜受益于聚合所有区域**（全区域平均反而更好），但随机裁剪在较小top-k（如k=2~5）表现最佳，使用所有区域会引入噪声导致性能下降。
- **在FGVC Aircraft上性能较低**（最高约51%），表明CLIP在该领域的表示能力不足，是当前方法的瓶颈。

## 7. 优点
- **轻量无监督**：无需真实标签、无需训练复杂定位模块，仅需冻结CLIP/SAM和训练一个线性分类头。
- **系统性比较**：首次在统一框架下对比多种评分策略和两种区域生成方式，揭示出反直觉的发现（随机裁剪优于SAM）。
- **伪标签策略有效**：提出的全局/局部伪标签使得方法在完全无标签场景下仍能逼近有监督性能，实用性强。
- **分析深入**：消融了top-k影响、真实标签与伪标签差距，并定性可视化（如图2），解释不同方法的行为差异。

## 8. 不足与局限
- **依赖CLIP表示质量**：在FGVC Aircraft此类CLIP预训练数据中覆盖不足的领域，性能下降严重（~51%），未探索域自适应或微调策略。
- **计算开销**：SAM生成掩膜耗时是随机裁剪的10倍以上，但最终性能却更差，实际部署性价比低。未提供具体时间数值。
- **实验覆盖有限**：未测试不同CLIP骨干（如ViT-L/14）、不同SAM版本、其他区域生成策略（如滑动窗口）。温度τ仅用于定义，未做敏感性分析。
- **偏差风险**：所有数据集中图像多为中心拍摄、背景较干净，未在具有复杂背景或遮挡的场景下验证。
- **应用限制**：需要CLIP文本头提供类别名称，无法处理未见类别或开放集场景；仅适用于类别已知的封闭集细粒度分类。

（完）
